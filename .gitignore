node_modules/
src/data/*.db
src/data/*.db-wal
src/data/*.db-shm
# Learned recipes and snapshots are kept by default (they ARE the knowledge base).
# Uncomment to treat them as regenerable cache instead:
# src/data/learned/
# src/data/snapshots/

================================================================================
 TODO — Community College Searcher
 Written: 2026-07-26 (after the Fall 2026 refresh)
================================================================================

DONE 2026-07-26 (for context)
  - Full re-scrape of all 118 colleges for Fall 2026: 33,141 courses
    (17,209 in person / 14,804 online / 1,128 hybrid), zero stale
    Spring 2026 / Fall 2025 rows left.
  - CVC online supplement run (+2,706 online courses across 27 colleges).
  - Organized folders rebuilt (72 colleges with full real schedules).
  - Supabase restored + refreshed dataset pushed; web app verified in the
    browser end-to-end (page loads, Fall 2026 results render, no errors).

--------------------------------------------------------------------------------
 1. RE-SCRAPE THE 9 EMPTY COLLEGES  (~mid-August 2026)
--------------------------------------------------------------------------------
These returned 0 courses on 2026-07-26 for legitimate reasons, not bugs.
Re-run them once their Fall listings appear (check weekly until full):

    npm run scrape -- solano-community-college cypress-college \
      feather-river-college mendocino-college mt-san-jacinto-college \
      napa-valley-college riverside-city-college skyline-college

  - solano-community-college .. Banner portal was in summer maintenance
                                (ssb.solano.edu redirected to a maintenance
                                page). Was previously live with real modality —
                                should come back on its own.
  - cypress / feather-river / mendocino / mt-san-jacinto / napa-valley /
    riverside-city / skyline .. CVC Exchange showed "0 courses" for them
                                (Fall sections not posted to the exchange yet).
  - calbright-college ......... permanently empty: not on CVC, no scrapable
                                schedule. Nothing to do.

Then push to Supabase again (step 3).

--------------------------------------------------------------------------------
 2. KEEP THE DATA FRESH THROUGH THE SEMESTER
--------------------------------------------------------------------------------
Schedules keep changing until (and after) classes start:

  - Re-run the full pipeline every few weeks during Aug-Sep:
        npm run scrape
        npm run cvc:supplement
        npm run cid:fetch         # refresh the C-ID transfer-approval CSVs
        npm run cid:supplement    # tag transfer-approved courses + backfill descriptions
        npm run organize
    (Always run cid:supplement AFTER scrape — a scrape rewrites each course's
     `meta`, which would otherwise drop the C-ID tags.)
  - Around Oct-Nov 2026: Spring 2027 registration opens. The Banner adapter
    auto-picks the newest open term, so a scrape then will start returning
    Spring 2027 — decide whether that's wanted or whether to pin Fall 2026
    (would need a small change to chooseTerm() in
    src/scraper/adapters/banner.js to prefer a target term by name).

--------------------------------------------------------------------------------
 3. PUSH TO SUPABASE AFTER EVERY LOCAL REFRESH
--------------------------------------------------------------------------------
The deployed site reads Supabase, not the local SQLite file. After any
scrape, publish with:

    node --env-file=web/.env.local src/migrateToSupabase.js

  - NOTE: `source web/.env.local` breaks in zsh (unquoted `&` in a value on
    line 6) — use Node's --env-file flag as above, or quote that value.
  - NOTE: the free-tier Supabase project auto-pauses after ~1 week of
    inactivity ("tenant not found" from the pooler = paused). Restore it in
    the Supabase dashboard first. Regular pushes also keep it awake.

--------------------------------------------------------------------------------
 3b. TRANSFER / "ACCEPTABLE" DATA  (added 2026-08-16)
--------------------------------------------------------------------------------
  - C-ID (Course Identification Numbering System) is now wired in as the
    authoritative "this course is accepted for transfer/articulation" signal:
        npm run cid:fetch        # downloads two public CSVs -> src/data/cid/
        npm run cid:supplement   # tags matched courses: meta.cId / cIdTitle /
                                 # cIdApproved, and backfills a description from
                                 # the C-ID descriptor when the course had none.
    ~7,700 courses across ~98 colleges match a C-ID. Surfaced in the web app as a
    "C-ID transfer-approved" filter + a "C-ID <num>" badge.
  - DONE 2026-09-13: UC transferability wired in via ASSIST.org. src/assist.js
    does the XSRF handshake; src/ucSupplement.js pulls each college's UC Transfer
    Course Agreement and tags meta.ucTransferable / csuTransferable.
        npm run uc:supplement            # all colleges (current academic year)
        npm run uc:supplement <slug>...  # specific colleges
        npm run uc:supplement --year 2026
    ~22.6k courses across 102 colleges tagged. Web app: "UC" transfer filter +
    "UC transferable" badge. RE-RUN AFTER EVERY SCRAPE (scrape rewrites meta).
    Handles the Foothill-De Anza campus-letter quirk (ANTH D001 <-> ANTH 1) via a
    de-lettered fallback match. TCA JSON cached to src/data/assist/<slug>.json.
  - Course-to-course articulation (a CCC course -> a *specific* UC campus course,
    /api/articulation/Agreements?Key=...) is still a future add; the UC TCA above
    already answers "does it transfer to UC at all".
  - DONE 2026-09-13: one row per teacher + notes out of titles. CVC courses with
    several instructors now split into one row per professor (was "A, B, C" in a
    single row); registration notes jammed into titles ("...-NOTE: <text>") moved
    to meta.note and shown on their own line. New scrapes do this automatically
    (src/scraper/cvc.js splitByProfessor + src/db.js splitTitleNote); existing
    data was fixed in place once via `node src/fixExisting.js`.
  - `npm run refresh` runs the whole pipeline in order (scripts/refresh.sh).

--------------------------------------------------------------------------------
 4. NICE-TO-HAVES / BACKLOG
--------------------------------------------------------------------------------
  - Two colleges lost their thin/low-quality data this refresh and now need a
    custom (headless-browser) scraper: city-college-of-san-francisco (site is a
    JS app; not really on CVC) and san-diego-college-of-continuing-education
    (sdcce.edu/coursenet is a JS app; its old rows had title="pdf"). Neither is
    on CVC, so there's no cheap fallback.
  - Find more Banner colleges: only the ones linking Banner from their
    homepage were auto-detected. Probing reg-prod / ssb host patterns could
    add more colleges with REAL modality data for free (adapter exists).
  - Catalog-sourced colleges have real courses but default "in person"
    modality — real modality only comes from live schedules (Banner/CVC).
  - Port 3000 conflict: another project (Race Hawks backend_v2) often runs
    on :3000. Run this app's local server with PORT=3456 node src/server.js
    or the web app with: cd web && npx next dev -p 3457
  - Consider automating the refresh (cron / scheduled job) instead of
    running it by hand each time.

================================================================================
